// enum - enumeration
// object that stores some closely related values
// only used to signal other engineers
// that these values are closely related
export enum MatchResult {
  HomeWin = 'H',
  AwayWin = 'A',
  Draw = 'D',
}
